export declare const regUtils: {
    /** 是否为中文字符 */
    testChinese(str: string): boolean;
    /** 是否为Email */
    testEmail(str: string): boolean;
    /** 是否为数字 */
    testNumber(str: string | number): boolean;
    /** 是否为中文字符或英文字符 */
    testChEn(str: string): boolean;
};
